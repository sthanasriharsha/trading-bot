# bot/__init__.py
# This empty file tells Python that the `bot` folder is a "package"
# (a collection of modules you can import from).
# Without this file, Python won't recognize `bot` as importable.
